<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="xyz.itwill.mapper.CarriersMapper">
 	<select id="selectCarriersById" parameterType="int" resultType="Carriers">
 		select carrier_id,carrier_name from carriers  where carrier_id=#{carrierId}
 	</select>
 </mapper>